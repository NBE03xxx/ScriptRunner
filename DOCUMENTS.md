# DOCUMENTS.md — Script Runner

## プロジェクト概要
LAN 内のサーバー群に配置されたシェルスクリプトを、ブラウザ上の Web UI から管理・実行するためのツール。スクリプトの実行はローカルターミナルエミュレータで起動する方式（ptyxis, gnome-terminal など自動検出）。

## コードベース構造
```
ScriptRunner/
├── app/
│   ├── main.py          # FastAPI バックエンド（API + static マウント）
│   └── static/
│       ├── index.html   # フロントエンド HTML
│       ├── app.js       # フロントエンド JS（認証、一覧表示、実行、ホスト死活チェック等）
│       └── style.css    # テーマ切替対応のスタイルシート
├── bin/                  # 実行対象のシェルスクリプト配置ディレクトリ（config.json で変更可能）
├── config.json           # 起動設定ファイル（script_dir）
├── config.example.json   # 設定ファイルの例
├── .env                  # 環境変数（SCRIPT_RUNNER_TOKEN, UVICORN_PORT, DISPLAY, XAUTHORITY など）
├── white_list.txt        # ホワイトリスト（実行許可スクリプト名。不存在/空なら全許可）
├── DOCUMENTS.md         # このファイル
├── README.md
├── install.sh           # インストール・セットアップ用スクリプト（root不要、GUIセッションから実行）
├── uninstall.sh         # アンインストール用スクリプト（root不要）
└── requirements.txt
```

## ルール・制約
- バックエンドは `app/main.py` のみ。追加のモジュールは避ける。
- スクリプト配置ディレクトリは `config.json` で指定する。変更時はサーバー再起動が必要。
- フロントエンドは HTML / JS / CSS を分離（`app/static/` 配下）。
- パスは `BASE_DIR`/ハードコードパスではなく、`__file__` 相対で解決する。
- `bin/` ディレクトリに `.sh` を追加すれば UI に自動反映される。

## コーディングスタイル
- Python: PEP 8, docstring は関数・クラスに付ける
- JS: async/await、var/bind の代わりに const/let
- CSS: CSS Variables でテーマ管理（`:root` / `[data-theme="dark"]`）
- コメントは日本語。機能概要と意図を簡潔に記録する。

## API エンドポイント

| メソッド | パス | 説明 |
|----------|------|------|
| `GET` | `/api/scripts` | スクリプト一覧（`group`, `tags`, `executable` フィールド付き） |
| `GET` | `/api/read/{filename}` | スクリプト内容の取得 |
| `POST` | `/api/save/{filename}` | スクリプト保存（`.sh` のみ許可） |
| `POST` | `/api/create/{filename}` | スクリプト新規作成（`.sh` のみ、同名は上書きしない） |
| `DELETE` | `/api/delete/{filename}` | スクリプト削除 |
| `POST` | `/api/execute/{filename}` | スクリプトを実行（allowlist制限あり）。ローカルターミナルで起動。 |
| `GET` | `/api/ping` | 全スクリプトのホストに対する SSH ポートチェック |
| `GET` | `/api/token-required` | トークン認証が有効かどうかを返す |

## 実装ポイント
- **認証**: `X-Secret-Token` ヘッダーによるトークン認証。`SCRIPT_RUNNER_TOKEN`（`.env`）が未設定の場合は認証がスキップされるため、**お一人様用のクライアント PC 前提**として扱うこと（LAN 内の誰でも操作可能になる）。
- **ホスト解析**: `parse_script_info()` がスクリプト内の `ssh user@host` 形式の行から接続先を抽出する（コメント行は対象外）。抽出できたホストに対して `/api/ping` が SSH 22 番の TCP 接続チェックを行う。
- **Group / Tag解析**: `parse_script_info()` は先頭30行だけをメタデータ解析対象とし、`# group: 名前` と `# tag: 名前` を大文字・小文字を区別せず認識する。値の前後空白と空値を除外し、Groupは最初の有効値、Tagは初出順で完全一致の重複を除いた値を採用する。日本語も利用できる。SSHホスト解析は従来どおり全文が対象となる。
- **Group / Tag UI**: カード内に補助情報として省略可能な1行表示を行う。Group昇順・降順ではGroup未指定を末尾に置き、同一Groupは名前順にする。Tagドロップダウンは一覧データから重複なしで生成し、Tag絞り込み後に現在の条件でソートして描画する。
- **スクリプト実行**: `POST /api/execute/{filename}` は利用可能なターミナルエミュレータ（kitty, alacritty, foot, gnome-terminal, xfce4-terminal, konsole, xterm, ptyxis の優先順で `shutil.which()` 検出、Wayland ネイティブ優先）を `subprocess.Popen()` で起動する方式。起動のみで結果ポーリングは行わない。`_detect_gui_env()` が `(display, xauth, wayland_display)` を自動検出し（環境変数 → `/run/user/$UID/wayland-*` → Mutter/Xwayland 認証ファイル → ~/.Xauthority）、Wayland セッションでは `WAYLAND_DISPLAY` と `XDG_SESSION_TYPE=wayland` を、X11 のみ環境では `DISPLAY` と `XDG_SESSION_TYPE=x11` を子プロセスに設定する（**Wayland 正式サポート、X11 はフォールバック**）。加えて GTK/Wayland アプリに必要な `DBUS_SESSION_BUS_ADDRESS`, `XDG_RUNTIME_DIR`, `XDG_SESSION_ID`, `GDK_BACKEND`, `LANG` などを継承する。
- **新規ファイル作成**: UIの「新規作成」からファイル名を入力し、本文へ `#!/bin/bash` が初期入力された状態で作成できる。`POST /api/create/{filename}` は排他的にファイルを作成し、同名ファイルを上書きしない。`POST /api/save/{filename}` は引き続き既存ファイルの上書き専用とする。

## ホワイトリスト機能
- プロジェクトルートの `white_list.txt` に許可するスクリプト名を1行に1つずつ記述する（`#` 始まりの行はコメント）。
  - ファイル不存在・空ファイルの場合は全スクリプトの実行が許可される。
  - `none` のみ記載の場合は一切実行できない。
- `/api/scripts` のレスポンスには各スクリプトの `executable: true/false` が含まれ、UI で無許可のスクリプトは Run ボタンが無効化される。

## `/api/scripts` の Group / Tag フィールド
- `group`: 文字列。未指定時は空文字 `""`。
- `tags`: 文字列配列。未指定時は空配列 `[]`。
- 既存の `name`, `path`, `host`, `mtime`, `executable` は変更しない。

## Group / Tag変更のテスト結果（2026-08-23）
- `python3 -m unittest discover -s tests -v`: 既存5件と追加6件の計11件が合格。未指定時の既定値、最初の有効Group、複数・重複Tag、日本語、30行制限、SSH全文解析、ホワイトリスト、API既存フィールド維持を確認した。
- `python3 -m py_compile app/main.py`、`node --check app/static/app.js`、`git diff --check`: 合格。
- UIコード上で、Tag絞り込み後に選択中条件でソートして描画する処理、Group昇順・降順、Tag選択イベント、長文省略表示、既存の1〜3列ブレークポイント、テーマ変数の継続利用を確認した。
- アプリ内ブラウザから一時ローカルサーバーへ接続できなかったため、今回の実画面操作による回帰確認は未実施。UIの新規作成・編集・削除・実行処理自体には変更を加えていない。

## 開発フロー
1. インストール: `./install.sh` （GUIセッションから実行、root不要）
2. `http://localhost:8080`（または `.env` の `UVICORN_PORT`）で確認
3. API の詳細は上の表を参照

## スクリプト
- `install.sh` — venv作成、config.json/.env生成、systemdユーザーサービス定義の生成・配置・有効化・起動。WAYLAND_DISPLAY / DISPLAY / XAUTHORITY の自動検出（Wayland 優先、X11 フォールバック）も行う（root不要）。表示言語はロケールから日本語・英語を自動判定する。
- `uninstall.sh` — サービス停止・無効化、プロジェクトディレクトリの削除確認（root不要）。インストーラーと同じ言語判定を使用する。

### インストーラーの表示言語
- 判定優先順位は `SCRIPT_RUNNER_LANG`（`ja` / `en`）→ `LC_ALL` → `LC_MESSAGES` → `LANG`。
- ロケール名が `ja` で始まる場合は日本語、それ以外または未設定時は英語を使用する。
- 端末メッセージ、確認プロンプト、Zenity/KDialogのタイトルを同じ言語に統一する。

## 実装仕様: インストール時のスクリプトフォルダー選択

### 調査結果
- **実現可能**。`app/main.py` は `config.json` の `script_dir` を `os.path.join(PROJECT_DIR, ...)` で解決しており、値が絶対パスの場合はその絶対パスがそのまま採用される。バックエンド側の大幅な変更は不要。
- Linux のシェルスクリプトからは `zenity --file-selection --directory`、KDE 環境では `kdialog --getexistingdirectory` を使って GUI のフォルダー選択ダイアログを表示できる。
- GUI ダイアログを利用できない環境（ツール未導入、SSH/ヘッドレス実行など）を考慮し、端末でのパス入力へフォールバックする必要がある。
- 選択先がプロジェクト外の場合、アンインストールでそのフォルダー自体を削除してはならない。現在の `uninstall.sh` は `${INSTALL_DIR}/bin` 固定で確認・バックアップしているため、`config.json` の設定値を参照するよう修正が必要。

### 実装内容
1. `install.sh` のファイル配置フェーズで `${INSTALL_DIR}/bin` を先に作成してから、「スクリプトを保存するフォルダー」を選択させる。初期位置および既定値は `${INSTALL_DIR}/bin` とする。
2. GUI 選択は `zenity` → `kdialog` の順で利用可能なコマンドを検出する。どちらも使えない場合は端末入力を使う。キャンセル時は既定値を使うかインストールを中止するかを明示的に確認する。
3. 選択結果は絶対パスへ正規化し、空文字、NUL、通常ファイルへの指定、読み書きできない既存ディレクトリを拒否する。存在しないパスは作成確認後に `mkdir -p` する。
4. `config.json` の `script_dir` に選択した絶対パスを JSON として安全に書き込む。空白、日本語、引用符を含むパスでも壊れないよう、文字列連結ではなく Python の `json` モジュール等で生成する。
5. 再インストール時は既存 `config.json` の `script_dir` を初期値として提示し、ユーザーが変更しない限り現在の保存先を維持する。
6. インストール完了時に、選択したフォルダーと設定ファイルの保存先を表示する。サービス起動前にディレクトリの存在・読み取り・書き込みを検証する。
7. `uninstall.sh` は `config.json` から `script_dir` を読み取り、対象スクリプトの確認とバックアップに使用する。プロジェクト外のフォルダーはバックアップ対象として案内するだけで、アンインストール時に削除しない。`${INSTALL_DIR}/bin` の場合のみ、従来どおりプロジェクト削除に含まれる旨を警告する。
8. `README.md` のインストール手順、ディレクトリ構造、アンインストール時の説明を新仕様に合わせて更新する。

### テスト項目
- 既定の `${INSTALL_DIR}/bin`、ホーム配下の任意フォルダー、空白・日本語を含むパスを選択して一覧取得・編集・実行できること。
- `zenity`、`kdialog`、GUI ツールなしの各経路でインストールを完了できること。
- ダイアログのキャンセル、存在しないパス、通常ファイル、権限不足のパスを安全に処理できること。
- 再インストールで既存の `script_dir` とスクリプトを保持できること。
- アンインストールでプロジェクト外のスクリプトフォルダーおよび内容が削除されないこと。
- `config.json` に相対パスを指定する従来方式との互換性が維持されること。

## 調査仕様: 大画面向けレスポンシブ複数列レイアウト

### 結論
- **修正可能**。現状は `/api/scripts` から取得した全件を `app/static/app.js` の `render()` が `<ul class="script-list">` に一括描画し、`app/static/style.css` が幅 `max-width: 820px` の1列として表示している。バックエンド API やデータ形式を変更せず、主に HTML / CSS と一部 JavaScript の変更で1〜3列化できる。
- 最大の制約は、現在のカード内に「スクリプト名、ホスト、状態、Run、Edit、Delete」が横並びで入る点である。現在のボタン表記のまま3列化すると、列幅不足による折り返しやカード高の不揃いが起きやすいため、3列時はアクションをアイコン化する方針が妥当である。
- 「1列あたりの行数を画面サイズに応じて変える」ことも可能。ただし、単純な CSS Grid は左から右への行単位配置となり、画面高を基準に列を埋める機能ではない。列ごとの件数まで画面高に合わせる場合は JavaScript で利用可能な高さとカード高を測定し、表示行数を算出する必要がある。

### 推奨レイアウト方針
1. `.container` の固定上限 `820px` を、画面幅に応じた上限（例: `min()` または十分に広い `max-width`）へ変更し、大画面の左右余白を一覧表示に利用する。
2. `.script-list` を CSS Grid に変更し、カードの最低実用幅を保ちながら1〜3列へ切り替える。目安は小画面で1列、中画面で2列、大画面で3列とし、最終的なブレークポイントは実機でカード幅を確認して決定する。列数だけを固定幅で判定するより、コンテナ幅を基準にした container query、または `repeat(auto-fit, minmax(...))` と最大3列の制御が適する。
3. 一覧順は現在の並べ替え結果を維持し、通常の読み順である「左から右、上から下」の行方向配置を基本とする。高さ優先で「上から下、次の列」へ流す方式は、列数変更時に項目位置が大きく変わり、目的のスクリプトを追いにくいため既定案にはしない。
4. 3列時は Run / Edit / Delete をそれぞれ意味の明確なアイコンボタンにする。文字は視覚上隠しても、各ボタンに `aria-label` を設定し、マウスの hover とキーボードの focus の両方でツールチップを表示する。ブラウザ標準の `title` だけに依存せず、CSSで即時表示できるツールチップを用意する。タッチ操作には hover がないため、アイコン自体で意味が判別できることも必要である。
5. 実行不可の Run は現状どおり `disabled` にし、ロック状態をアイコンまたは補助表示で区別する。無効ボタンは hover イベントを受けない場合があるため、ツールチップはボタンを包む要素に持たせる。
6. ファイル名とホスト名は省略表示（ellipsis）を基本とし、hover / focus で全文を確認できるようにする。カード高を揃えることで、複数列でも行の対応関係と視線移動を安定させる。

### 1列あたりの表示行数
- 推奨する第一段階は、列数を画面幅から1〜3列に決め、行数を `ceil(スクリプト数 / 列数)` として自然に変化させる方式である。画面が広いほど列数が増えて1列あたりの件数が減るため、「大画面を有効活用する」という主目的を、複雑な状態管理なしで満たせる。全件が同じページに残り、現在の操作感も維持できる。
- 画面の**高さ**に応じて「1列あたりに収める件数」も厳密に変える場合は、ヘッダー、コンテナ余白、カード高、カード間隔から `floor(利用可能高 / 1行高)` を JavaScript で計算する。`ResizeObserver` と `resize` 時の再計算を使い、フォント読込後やモバイル回転後にも更新する。
- 高さ基準方式で、算出した行数 × 3列を超えるスクリプトがある場合は、4列目を作って横スクロールさせず、ページ切替または「さらに表示」が必要になる。これは一覧性と操作状態が変わるため、初回実装では採用せず、スクリプト数が非常に多く実際に必要と判明した段階で追加するのが安全である。
- CSS Multi-column は列ごとの件数を自動調整できるが、カードの読み順、途中分割、操作要素のフォーカス順を制御しづらいため、このUIには推奨しない。

### 変更対象（実装時）
- `app/static/style.css`: コンテナ幅、1〜3列グリッド、列幅別カード構成、アイコンボタン、ツールチップ、長い文字列、省略表示、キーボードフォーカスのスタイル。
- `app/static/app.js`: アクションボタンをアイコン＋アクセシブルなラベルで生成する処理。高さ基準の表示件数を採用する場合のみ、行数計算とリサイズ監視も追加する。
- `app/static/index.html`: 必要に応じて一覧の説明やアクセシビリティ用文言を追加し、CSS / JS のキャッシュバスターを更新する。
- `app/main.py`: 原則として変更不要。

### 評価
- **実現性: 高い**。一覧データと操作APIは表示レイアウトから独立しており、フロントエンド内で完結できる。
- **大画面の利用効率: 高い**。3列では同じスクロール量で最大約3倍の項目を俯瞰できる。ただし、3列を成立させる最低幅を下回る画面で無理に適用しないことが重要である。
- **視認性: 条件付きで良好**。カード高の統一、名称の省略表示、3列時のアイコン化、十分な列間隔を組み合わせれば維持できる。アイコンだけでは意味が曖昧になり得るため、ツールチップと `aria-label` は必須とする。
- **操作性: 良好**。並び順を行方向に維持すればレスポンシブ切替後も自然に探せる。hover 専用の説明はキーボード・タッチ環境で不足するため、focus 対応と認識しやすいアイコンが必要である。
- **保守性: 良好**。第一段階をCSS中心のレスポンシブ列化に留めれば影響範囲が小さい。高さ基準の件数制御やページ切替は複雑性が増すため、別段階に分けるのが望ましい。

### 推奨する実装順序と確認項目
1. まずコンテナを広げ、幅に応じた1〜3列とカード高の統一を実装する。
2. 3列時のアクションをアイコン化し、hover / focus ツールチップ、`aria-label`、実行不可表示を追加する。
3. 代表的な幅（スマートフォン、ノートPC、Full HD、WQHD、4K）とライト / ダークテーマで、長いファイル名・長いホスト名・ホスト不明・Run無効を確認する。
4. スクリプト件数が1件、列数未満、列数ちょうど、列数を超える場合に、並び順と空き領域が不自然でないことを確認する。
5. キーボードのみの操作、200%ズーム、タッチ相当環境でボタンの意味とフォーカス位置を確認する。
6. 実測後も縦方向の空間利用が不足する場合に限り、高さ基準の行数計算とページ切替を第二段階として評価する。

### 実装後評価（2026-08-23）
- 第一段階として、1050px未満は1列、1050px以上は2列、1550px以上は3列となるCSS Gridを実装した。9件のテストデータでは、390px幅で9行、1280px幅で5行、1920px幅で3行となり、列数に応じて1列あたりの表示件数が自然に変化することを確認した。
- 各幅で横スクロールが発生しないことを確認した。カード幅は390px画面で318px、1280px画面で582px、1920px画面で559pxとなり、情報と操作領域を実用的な幅に保てている。
- 3列時のみ Run / Edit / Delete の文字を視覚的に隠し、アイコン表示へ切り替わることを確認した。マウス操作に加えてボタンfocusでもツールチップが表示され、実行不可のRunはラッパーをfocus可能にすることで説明を確認できる。
- JavaScript構文検査と `git diff --check` は合格した。リポジトリに `tests/` ディレクトリが存在しないため、既存の自動テストスイートは実行できなかった。
- 評価結果は良好であり、高さ基準のページ分割を追加しなくても大画面活用という目的を満たしている。現時点では全件表示を維持し、ページ切替は導入しない。

## 実装仕様: フォルダー選択画面を開く前の確認

### 調査結果と設計
- **修正可能**。従来は端末に「シェルスクリプトを保存するフォルダーを選択してください」と表示した直後にGUIダイアログを開くため、ダイアログが端末を覆い、案内を読む時間がなかった。
- GUIフォルダー選択を利用できる場合は、「スクリプトの保存先を選択します。準備ができたら Enter キーを押してください:」と端末に表示し、Enter入力後にダイアログを開く。操作対象が端末からGUIへ移ることを利用者自身のタイミングで確認できる。
- Enter待ちは標準入力が対話端末（`-t 0`）の場合だけ行う。非対話実行では停止せず、「フォルダー選択画面を開きます...」と表示して従来どおり起動する。
- GUIツールがない場合は、従来どおり端末で直接パスを入力する。この経路にはEnter待ちを追加しない。
- Zenityは `--title` でタイトルを「スクリプトを保存するフォルダーを選択」（英語では「Select the script storage folder」）へ変更する。Zenity 4.2.1の一般ヘルプには `--ok-label` / `--cancel-label` が掲載されるが、ファイル選択ダイアログへ指定すると `--ok-label is not supported for this dialog` で起動に失敗するため、ボタンは標準表示を維持する。
- KDialogも既存の `--title` を継続利用して同等のタイトルを表示する。ツール差異への対応でコードを複雑にしないため、ボタンは標準表示を維持する。

### 評価項目
- 日本語・英語の対話端末で、Enterを押すまでZenity／KDialogが開かないこと。
- Enter後に選択画面が一度だけ開き、選択、キャンセル、既定フォルダー利用の既存フローが維持されること。
- 非対話標準入力ではEnter待ちをせず、GUI選択処理へ進むこと。
- GUIツールなしでは余分な確認を挟まず、従来どおり端末のパス入力へ進むこと。
- 空白や日本語を含む既定パスが選択画面の初期位置として維持されること。

### 実装後評価（2026-08-23）
- 疑似端末で、案内表示後にEnterを押すまで処理が停止し、Enter入力後に再開することを確認した。
- 非対話標準入力では待機せず、日本語では「フォルダー選択画面を開きます...」、英語では「Opening the folder selection dialog...」を表示して選択処理へ進むことを確認した。
- Zenityを模擬した分岐テストで、日本語・英語のタイトルが正しく渡ることを確認した。決定・キャンセルボタンの文言指定はZenity 4.2.1のファイル選択ダイアログで非対応であるため使用しない。
- 修正後の実機テストでは、タイトルと初期フォルダーだけを指定したZenityのファイル選択ダイアログが正常に起動し、2秒の自動タイムアウトまでエラーなく表示された（タイムアウト時の正常な終了コード5）。
- 選択結果として空白を含む英語パスと、空白・日本語を含むパスが正規化後も欠落せず `SCRIPT_DIR` に設定されることを確認した。
- `bash -n install.sh` と `git diff --check` は合格した。実際のフォルダー選択操作はインストール処理やGUIセッションへ影響するため、関数単位の模擬テストで評価した。

## 調査・実装仕様: Web UIからのスクリプト新規作成

### 結論
- **実装可能**。現状は `app/static/app.js` の編集モーダルが既存ファイルの読込・保存に対応し、`app/main.py` の `POST /api/save/{filename}` は誤った新規作成を防ぐため既存ファイルだけを上書きしている。既存の編集フローを維持したまま、新規作成専用APIとファイル名入力を追加すれば実現できる。
- 新規作成ボタンをクリックした直後の本文は、余計な説明文やサンプル処理を加えず `#!/bin/bash` だけを入力済みとする。保存内容としては末尾の改行を1つ含む `#!/bin/bash\n` を初期値とし、エディタ上では1行目にシェバン、2行目が空の状態で入力を開始できるようにする。
- 作成後のスクリプトは現在の一覧・編集・削除・実行処理をそのまま利用できる。実行時はバックエンドが `bash <script_path>` で起動するため、作成時に実行権限を付ける必要はない。

### UI設計
1. ヘッダーの操作エリアに「新規作成」ボタンを追加する。画面幅が狭い場合もテーマ切替・並べ替えと重ならないよう、既存のレスポンシブ配置に組み込む。
2. ボタン押下時は既存のエディタモーダルを新規作成モードで開き、タイトルを「スクリプトの新規作成」、本文を `#!/bin/bash\n` にする。前回編集した内容やファイル名は残さない。
3. 新規作成モードでのみファイル名入力欄を表示する。利用者は `.sh` を含むファイル名を入力し、空欄のまま保存はできない。入力欄には例として `example.sh` を表示するが、実際のファイル名は自動入力しない。
4. 保存ボタン押下時、新規作成モードでは作成APIを呼び、編集モードでは従来どおり保存APIを呼ぶ。作成成功後はモーダルを閉じ、一覧を再取得して作成したスクリプトを表示する。
5. ファイル名不正、同名ファイル存在、保存先の書込失敗について、APIの `detail` を日本語でモーダル内またはステータス表示へ提示する。単一の「保存に失敗しました」だけにせず、利用者が修正方法を判断できるようにする。
6. モーダルを閉じた場合は入力途中の内容を破棄する。既存仕様と同じ挙動だが、誤操作を減らす必要が生じた場合は、別段階で未保存変更の確認を検討する。

### API・バックエンド設計
1. 新規作成専用として `POST /api/create/{filename}` を追加し、JSON本文の `content` を保存する。既存の `POST /api/save/{filename}` は既存ファイル専用のまま維持し、作成と編集の意図を分離する。
2. ファイル名は前後の空白を許可せず、`.sh` 拡張子を必須とする。空文字、`.`、`..`、パス区切り文字を含む名前、制御文字を拒否し、`resolve_safe_path()` による保存先ディレクトリ外への脱出防止も併用する。初回実装ではサブディレクトリ作成を対象外とする。
3. 同名ファイルが存在する場合は `409 Conflict` を返し、上書きしない。存在確認後の競合でも上書きが起きないよう、排他的作成モード（Pythonの `open(..., "x", encoding="utf-8")`）を使用する。
4. `SCRIPT_DIR` が存在しない場合は `404`、ディレクトリでない場合または書込不能の場合は適切なエラーを返す。API例外の詳細にOS内部のパスや不要な情報を露出させない。
5. `content` が文字列であることをリクエストモデルで検証する。将来の項目追加とエラー形式の統一を考慮し、既存の未型付け `dict` ではなく Pydantic モデルを作成APIに使用する。
6. 作成APIも既存ミドルウェアの `X-Secret-Token` 認証対象とする。ホワイトリストが限定指定されている場合、新規ファイルは一覧に表示・編集できるが、`white_list.txt` に同名がなければ現行仕様どおり Run は無効になる。ホワイトリスト自体は自動変更しない。

### 変更対象（実装時）
- `app/main.py`: 作成リクエストモデル、ファイル名検証、新規作成API、競合・書込エラー処理を追加する。
- `app/static/index.html`: 新規作成ボタン、エディタ内のファイル名入力欄、必要なエラー表示領域を追加し、CSS / JavaScript のキャッシュバスターを更新する。
- `app/static/app.js`: 新規作成モードの開始・終了、初期本文の設定、ファイル名検証、作成API呼出し、APIエラー詳細の表示を追加する。既存編集モードとの状態混在を防ぐ。
- `app/static/style.css`: 新規作成ボタン、ファイル名入力欄、狭い画面での操作エリアを調整する。
- `README.md`: 実装時に新規作成手順とAPI説明を追記する。

### テスト項目
- 新規作成ボタンを押すたびに、本文が正確に `#!/bin/bash\n` で始まり、以前の編集内容が混入しないこと。
- 正常な `.sh` ファイル名で作成でき、一覧へ反映され、再読込後も内容が `#!/bin/bash\n` のままであること。
- 空欄、`.sh` 以外、前後空白、パス区切り、`.` / `..`、制御文字を含むファイル名を拒否すること。
- 同名ファイルに対して `409` を返し、既存内容を一切変更しないこと。並行した2件の作成要求でも片方だけが成功すること。
- トークン未送信・不一致時に作成できないこと。限定ホワイトリスト利用時は作成後の Run が無効で、一覧・編集・削除は可能であること。
- 保存先不存在・書込不能時に理解可能なエラーが表示され、空または壊れたファイルを残さないこと。
- 作成後に編集・削除が従来どおり動作し、既存ファイルの編集フローに回帰がないこと。
- スマートフォン幅、ライト / ダークテーマ、キーボード操作で新規作成ボタン、ファイル名入力、保存、閉じるを利用できること。
- Python / JavaScript の構文検査、既存テスト（存在する場合）、`git diff --check` が合格すること。

### 評価
- **実現性: 高い**。既存エディタと一覧更新処理を再利用でき、変更はバックエンド1エンドポイントとフロントエンドの状態分岐が中心となる。
- **安全性: 高い**。新規作成専用API、ファイル名制限、排他的作成により、既存スクリプトの意図しない上書きと保存先外への書込みを防げる。
- **互換性: 高い**。既存の読込・上書き・実行・削除APIを変更せず追加実装できる。作成直後の実行可否も現在のホワイトリスト規則に自然に従う。

### 実装後評価（2026-08-23）
- ヘッダーに「新規作成」ボタンを追加し、既存エディタを新規作成モードで再利用した。ボタンを押すたびにファイル名と以前の編集状態を初期化し、本文へ正確に `#!/bin/bash\n` を設定する。
- `POST /api/create/{filename}` を追加した。`.sh` 拡張子、前後空白、パス区切り、制御文字等を検証し、`open(..., "x")` による排他的作成で既存ファイルを保護する。作成途中の書込エラー時は作成済みファイルの除去を試みる。
- 新規作成時はAPIの日本語エラー詳細をモーダル内とステータス領域へ表示する。スマートフォン幅では新規作成とテーマ切替を同じ行で利用できるよう調整した。
- 自動テストを5件追加し、初期シェバンの保存、既存ファイル保護、2件の並行作成で一方のみ成功、不正ファイル名拒否、保存先不存在時の404を確認した。全5件が合格した。
- Python / JavaScriptの構文検査と `git diff --check` が合格した。

## 変更ログ
- **2026-08-23**: 機能追加。シェルスクリプト冒頭コメントによるGroup / Tag分類、APIフィールド、カード表示、Groupソート、Tagドロップダウン絞り込みを追加。メタデータ/APIの自動テスト6件を追加し、SSH全文解析とホワイトリスト動作の互換性も確認。
- **2026-08-23**: 機能追加。Web UIからのスクリプト新規作成を実装。本文の `#!/bin/bash` 初期入力、ファイル名検証、同名ファイルを上書きしない排他的作成、APIエラー表示、スマートフォン向け配置、自動テスト5件を追加。CSS / JavaScriptキャッシュバスターを `?v=12` / `?v=11` に更新。
- **2026-08-23**: 調査・設計。Web UIからのスクリプト新規作成は実装可能と判断。新規作成ボタン、`#!/bin/bash` の初期入力、ファイル名入力、新規作成専用API、同名ファイルの排他的な上書き防止、検証・テスト方針を実装仕様として追記。
- **2026-08-23**: 修正。Zenity 4.2.1のファイル選択ダイアログでは `--ok-label` が非対応で起動に失敗するため、決定・キャンセルボタンの文言指定を削除。Enter待ちとタイトル指定は維持。
- **2026-08-23**: UI 改善。インストール時のGUIフォルダー選択前に、対話端末でEnter入力を待つ案内を追加。Zenity／KDialogのタイトルを日本語・英語で明示し、非対話実行とGUIなしの端末入力は停止しない。
- **2026-08-23**: UI 改善。画面幅に応じてスクリプト一覧を1〜3列へ切り替えるレスポンシブグリッドを実装。大画面ではカード内操作をアイコン化し、hover / focus ツールチップと `aria-label` を追加。長いスクリプト名・ホスト名の省略表示、実行不可ボタンのキーボード説明にも対応。CSS / JavaScript キャッシュバスターを `?v=11` / `?v=10` に更新。
- **2026-08-23**: 修正。ホスト抽出を端末ラッパー経由の `ssh`（例: `ptyxis --new-window -- ssh user@host`）に対応。起動時はスクリプト一覧の描画後に死活確認を開始し、先に取得済みの結果も再描画後に反映する。不明なホストはオフラインではなく「不明」と表示する。JavaScriptキャッシュバスターを `?v=9` に更新。
- **2026-08-23**: 機能追加。`install.sh` と `uninstall.sh` の表示をロケールに応じて日本語・英語へ自動切り替え可能にした。`SCRIPT_RUNNER_LANG=ja|en` による上書きにも対応。
- **2026-08-23**: 修正。初回インストール時のフォルダー選択ダイアログを、リポジトリ配置と `${INSTALL_DIR}/bin` 作成の後に表示する順序へ変更。Zenityが存在しない初期フォルダーについて警告する問題を解消。
- **2026-08-23**: 機能追加。インストール時に GUI ダイアログから任意のスクリプト保存フォルダーを選択可能にした。Zenity、KDialog、端末入力のフォールバック、パス検証、JSONの安全な生成、再インストール時の設定維持に対応。アンインストールは設定された保存先を参照してバックアップし、プロジェクト外のフォルダーを削除しない。
- **2026-08-23**: 修正。ステータスメッセージの表示をヘッダー右端スロット `#startup-message` のみに統一。`.container` 内の `#status-message` 要素（HTML・CSS）を削除し、`showStatus()` が `#startup-message` にのみ表示するよう変更。`.success` / `.error` のクラス定義を `#startup-message.success` / `#startup-message.error` に限定。キャッシュバスターを `?v=8` に更新。
- **2026-08-23**: 修正。起動メッセージ（`#startup-message`）の表示位置がヘッダー右端からずれる問題を解消。原因は `position: sticky` の `.app-header` が積層コンテキストを形成するため、内部の `position: absolute` がスクロール時にヘッダーと一体となって動いていたこと。`#startup-message` を `position: fixed`（画面右上、`top: 20px; right: 16px; z-index: 200`）に変更し、常時画面の右上に表示されるようにした。幅指定を `min(45vw, 320px)` に変更。キャッシュバスターを `?v=7` に更新。
- **2026-08-23**: Wayland 環境の正式サポート。ターミナル検出順を Wayland ネイティブ優先（kitty, alacritty, foot, gnome-terminal, xfce4-terminal, konsole, xterm, ptyxis）に変更し `foot` を追加。kitty は `--single-instance` で起動。`_detect_gui_env()` が `(display, xauth, wayland_display)` を返し、`/run/user/$UID/wayland-*` からの Wayland セッション検出と `XDG_SESSION_TYPE` の設定を追加（X11 はフォールバック）。`install.sh` の `.env` 生成も Wayland 優先の検出に修正。
- **2026-08-23**: ドキュメント整合。`OPENCODE.md` 記載を `DOCUMENTS.md` に修正、開発フローの URL を `.env` 基準に修正、「スクリプト実行アーキテクチャ」を「実装ポイント」として再構成（認証の前提・ホスト解析・新規ファイル作成不可を明記）。バックエンドのホスト解析をコメント行除外対応に改善し、`shutil` の重複 import を整理した。
- **2026-08-23**: 修正。`app.js` の読み出しにキャッシュバスター（`?v=6`）を追加し、`style.css` と合わせて `?v=6` に統一。これにより、更新後にブラウザが古い `app.js` をキャッシュから読み込むことで起動メッセージ（ヘッダー右端スロット）が表示されない問題が解消される。
- **2026-08-22**: UI 改善。ヘッダー（タイトル・テーマ切替・並び替え）を `position: sticky` でスクロール時も固定表示にし、起動メッセージ（「Script Runner に接続しました」）をヘッダー右端の常設スロット `#startup-message` に表示するよう変更。`showStatus()` が `.container` 内の `#status-message` とヘッダー右端スロットの両方に同期表示する（4 秒自動消滅、タイマーは 1 本に統合）。ヘッダー背景は body と同じ紙テクスチャ＋グラデーション（`background-attachment: local`）で継ぎ目を解消。600px 以下ではスロットをタイトルの下へ移動。CSS キャッシュバスターを `?v=5` に更新。
- **2026-08-15**: ドキュメント修正。ホワイトリスト記述を `white_list.txt` ベースに修正、systemdサービス定義（userスコープ）を install.sh が直接生成する方式であること等をコードと整合させた。
- **2026-08-15**: スクリプト実行を「ローカルターミナル起動」方式に変更（ptyxisなど7種対応）。DISPLAY/XAUTHORITY自動検出を追加（X11/Wayland対応）。systemdサービスをuserスコープへ変更。
- **2026-08-14**: `start.sh` と `stop.sh` を廃止。systemdサービスのみに統一。
- **2026-08-13**: 改善修正を反映
  - スクリプト実行を `asyncio.create_subprocess_exec()` に変更し、stdout/stderr/returncode を取得可能に（`exec_id` ベースの結果ポーリング）
  - ホワイトリスト機能 (`execution_allowlist`) を実装。UI で無許可スクリプトの Run ボタンが無効化されるように対応
  - `index.html` の不要な閉じタグを削除
  - `/api/ping` に SCRIPT_DIR 存在チェックを追加
  - `/api/save/{filename}` に `.sh` 拡張子バリデーションを追加
  - `natural_key_path` のソートキー比較エラー（int/str 混在）を修正
