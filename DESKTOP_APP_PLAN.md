# Script Runner デスクトップアプリ化計画

## 1. 目的

現在の Script Runner は、利用者がブラウザを起動し、`localhost:8080` を開いてからスクリプトを選択する構成である。本計画では外部ブラウザを使用せず、Ubuntu 26.04 上のデスクトップアプリとして次の操作で利用できる構成へ変更する。

1. アプリケーションメニューまたはデスクトップエントリーから Script Runner を起動する。
2. 最大化された専用ウィンドウでスクリプトを選択する。

現行の画面デザイン、スクリプト管理機能、Group / Tag、ホワイトリスト、死活確認等は可能な限り維持する。

## 2. 確定した前提

- 対象 OS は Ubuntu 26.04 LTS のみとする。
- GUI は GTK 4、埋め込み表示は WebKitGTK 6.0、Python バインディングは PyGObject を使用する。
- HTML / CSS / JavaScript で構成された現在の UI を WebKitGTK 内で再利用する。
- FastAPI バックエンドは systemd ユーザーサービスとして常駐させる。
- GTK / WebKitGTK アプリは必要なときだけデスクトップエントリーから起動する。
- GTK アプリを閉じてもバックエンドサービスは停止しない。
- バックエンドは LAN へ公開せず、ループバックインターフェースだけで待ち受ける。
- 待受ポートは固定せず、サービス起動時に利用可能な動的ポートを選択する。
- 接続情報は `$XDG_RUNTIME_DIR` 配下へ安全に書き出す。
- アプリケーションウィンドウは起動時に最大化する。
- スクリプトを実行するターミナルプロセスは GTK アプリ側から起動する。
- 通常利用では外部ブラウザ、URL入力、お気に入り、認証トークン入力を必要としない。
- Git 操作は利用者から改めて指示があるまで行わない。

## 3. 採用アーキテクチャ

```text
systemd --user
  └─ script-runner.service
       └─ FastAPI / Uvicorn
            ├─ 127.0.0.1 の動的ポートで待受
            ├─ スクリプト一覧・読込・作成・保存・削除
            ├─ Group / Tag・SSHホスト解析・死活確認
            └─ $XDG_RUNTIME_DIR/script-runner/connection.json

デスクトップエントリー
  └─ Gtk.Application
       └─ Gtk.ApplicationWindow（起動時最大化）
            └─ WebKit.WebView
                 ├─ connection.json を読み取ってサービスへ接続
                 ├─ 現行 HTML / CSS / JavaScript を表示
                 └─ Run 操作を GTK 側へ通知
                      └─ 現在の GUI セッションでターミナルを起動
```

常駐対象は FastAPI / Uvicorn のみとする。GTK、WebKitGTKおよびWebプロセスはウィンドウ表示中だけ動作させる。

## 4. プロセスごとの責務

### 4.1 FastAPIユーザーサービス

以下を担当する。

- `config.json` の読み込みとスクリプト保存先の解決
- `.sh` ファイルの一覧取得
- スクリプト内容の読込、排他的な新規作成、保存、削除
- Group / Tag、SSH接続先、更新日時の解析
- `white_list.txt` に基づく実行可否判定
- SSH 22番ポートの死活確認と短時間キャッシュ
- デスクトップアプリ向けセッションの検証
- ヘルスチェック情報の提供
- 動的ポートと接続情報の管理

以下は担当しない。

- GTKウィンドウの生成
- WebKitGTKの起動
- GUIセッションの検出
- ターミナルエミュレーターの起動
- `WAYLAND_DISPLAY`、`DISPLAY`、`XAUTHORITY` に依存する処理

### 4.2 GTKデスクトップアプリ

以下を担当する。

- `Gtk.Application` と一意なアプリケーションIDによる単一起動
- 2回目以降の起動要求で既存ウィンドウを前面へ表示
- `Gtk.ApplicationWindow` の生成と起動時最大化
- WebKitGTK 6.0による既存UIの表示
- `$XDG_RUNTIME_DIR` からの接続情報読込
- サービス未起動・起動途中・異常時の再試行とエラー表示
- WebKitGTKから受け取ったRun要求の検証
- 現在のWayland / X11セッション環境を継承したターミナル起動
- 外部URL遷移、ポップアップ、不要なダウンロード等の制限
- アプリ終了時のWebKit関連プロセスの終了

GTKアプリ終了時にFastAPIユーザーサービスを停止してはならない。

## 5. 動的ポートと接続情報

### 5.1 ランタイムディレクトリ

サービスは起動時に次のディレクトリを作成する。

```text
$XDG_RUNTIME_DIR/script-runner/
```

要件は次のとおりとする。

- 所有者は実行ユーザー本人とする。
- ディレクトリ権限は `0700` とする。
- 通常ファイル、シンボリックリンク、他ユーザー所有ディレクトリとの衝突を拒否する。
- `$XDG_RUNTIME_DIR` が未設定または不正な場合、サービスは安全側で起動失敗とする。
- ホームディレクトリや `/tmp` への代替保存は行わない。

### 5.2 接続情報ファイル

接続情報は次のファイルへ書き出す。

```text
$XDG_RUNTIME_DIR/script-runner/connection.json
```

記録する情報は以下を基本とする。

- スキーマバージョン
- サービスのプロセスID
- ホスト（常に `127.0.0.1`）
- 動的に割り当てられたポート
- 起動ごとに生成するランダムなセッショントークン
- サービス起動時刻またはインスタンスID

要件は次のとおりとする。

- ファイル権限は `0600` とする。
- 一時ファイルへ書き込んでから同一ディレクトリ内で原子的に置換する。
- JSON破損や途中書込みをGTKアプリが読み取らないようにする。
- サービス終了時に自身が生成したファイルだけを削除する。
- GTKアプリはPIDだけを信用せず、ヘルスチェックとインスタンスIDを照合する。
- 古い接続情報を検出した場合、一定回数再試行した後に利用者へエラーを表示する。

### 5.3 動的ポートの確保

- 待受アドレスはIPv4の`127.0.0.1`とする。
- OSに空きポートを割り当てさせる。
- ポート番号の決定とUvicornの待受開始の間に、別プロセスがポートを取得できる競合を作らない。
- 可能であれば、先にバインド済みソケットを作成し、そのソケットをUvicornへ引き渡す。
- 待受開始を確認してから`connection.json`を公開する。
- 接続情報公開後にサーバー起動が失敗した場合はファイルを削除する。

## 6. 認証とローカルセキュリティ

動的ポートでループバック待受にしても、同一ユーザーまたは同一端末上の別プロセスからアクセスされる可能性があるため、認証は維持する。

### 6.1 デスクトップモード

- サービス起動ごとに十分な長さのランダムトークンを生成する。
- トークンは`connection.json`にのみ保存し、権限を`0600`とする。
- GTKアプリは接続時に一時ブートストラップ処理を行う。
- FastAPIは検証後、`HttpOnly`かつ`SameSite=Strict`のセッションCookieを設定する。
- JavaScriptや`localStorage`には秘密情報を保存しない。
- GTKアプリ終了後もサービスが継続するため、セッションはサービスのインスタンス単位で管理する。
- サービス再起動後は以前のCookieや接続情報を無効とする。

### 6.2 従来Webモード

開発・診断用として従来Webモードを残す場合のみ、`X-Secret-Token`方式を互換機能として維持する。通常インストールではWebモードを有効化せず、LAN待受もしない。

### 6.3 WebKitGTK制限

- サービスの内部オリジン以外へのトップレベル遷移を拒否する。
- 新規ウィンドウとポップアップを拒否する。
- 不要なダウンロードを拒否する。
- リリース動作ではWeb Inspectorを無効化する。
- Google Fonts等の外部リソース依存を削除する。
- 必要な場合はContent Security Policyを追加する。

## 7. スクリプト実行フロー

Run操作は、バックエンドからではなくGTKアプリからターミナルを起動する方式へ変更する。

```text
利用者がRunを押す
  ↓
JavaScriptがWebKitGTKのメッセージハンドラーへ要求
  ↓
GTKアプリがファイル名と要求元を検証
  ↓
FastAPIへ最新の実行可否と安全な対象情報を照会
  ↓
GTKアプリが現在のGUI環境でターミナルを起動
```

要件は次のとおりとする。

- JavaScriptから任意コマンドを渡せない設計にする。
- 渡せる値はスクリプト識別子またはファイル名に限定する。
- 最終的なパス検証とホワイトリスト判定はFastAPI側でも行う。
- GTK側も返されたパスが設定済みスクリプトディレクトリ内であることを確認する。
- シェル文字列を組み立てず、引数配列でプロセスを起動する。
- ターミナルプロセスはGTKアプリから独立させ、アプリ終了後も継続可能にする。
- Waylandネイティブ対応ターミナルを優先する現在の順序は再評価して維持する。
- 従来の`POST /api/execute/{filename}`は、互換Webモードを残す期間だけ保持する。

## 8. GTKアプリの画面動作

### 8.1 起動

1. `Gtk.Application`を開始する。
2. 既存インスタンスがあれば、そのウィンドウを前面へ出して終了する。
3. 最大化されたウィンドウと起動中画面を表示する。
4. `connection.json`を読み取る。
5. ヘルスチェックを実施する。
6. 必要に応じて短い間隔で有限回再試行する。
7. 認証を確立してWebKitGTKへUIを読み込む。

### 8.2 接続失敗

次の状態を利用者が区別できるようにする。

- ユーザーサービスが起動していない。
- サービスがまだ起動処理中である。
- 接続情報が存在しない、または壊れている。
- 接続情報が古い。
- サービスへ接続できない。
- 認証に失敗した。
- `config.json`が不正である。
- スクリプト保存先が存在しない。

エラー画面には再接続、アプリ終了、ログ確認方法を提示する。GTKアプリから無条件にサービス設定を書き換えたり、破壊的な復旧処理を実行したりしない。

### 8.3 終了

- GTKウィンドウとWebKitプロセスを終了する。
- FastAPIサービスは停止しない。
- 起動済みターミナルとスクリプト処理は停止しない。

## 9. systemdユーザーサービス

サービス定義は以下の方針とする。

- ユーザー単位で起動する。
- `Restart=on-failure`を維持する。
- 標準出力・標準エラーはjournalへ送る。
- GUI環境変数を`.env`へ固定保存しない。
- 固定ポートや`UVICORN_PORT`を使用しない。
- `XDG_RUNTIME_DIR`はsystemdユーザー環境から取得する。
- サービス起動時に古い接続情報を安全に処理する。
- サービス停止時に自身の接続情報を削除する。
- `0.0.0.0`では待ち受けない。
- 標準では`loginctl enable-linger`を実行しない。
- ログイン中だけサービスを動作させ、ログアウト時に停止する。

サービスファイル名は既存の`script-runner.service`を基本的に維持する。名称変更が必要になった場合は、旧サービスの停止・無効化・削除を移行処理へ含める。

## 10. デスクトップエントリー

インストール時に以下をユーザー領域へ配置する。

```text
~/.local/share/applications/script-runner.desktop
~/.local/share/icons/hicolor/scalable/apps/script-runner.svg
```

デスクトップエントリーには次を設定する。

- `Type=Application`
- `Name=Script Runner`
- 日本語の`Comment`
- GTKアプリを起動する絶対パスの`Exec`
- `Icon=script-runner`
- `Terminal=false`
- `Categories=Utility;System;`
- `StartupNotify=true`
- GTKアプリケーションIDと整合する識別情報

デスクトップそのものへのショートカット配置は、Ubuntuのデスクトップ環境による信頼済み設定が必要になるため、標準ではアプリケーションメニューへの登録までとする。

## 11. Python・OS依存関係

Ubuntu 26.04のシステムパッケージを優先する。

主な依存関係は以下とする。

- `python3`
- `python3-gi`
- `python3-gi-cairo`
- `gir1.2-gtk-4.0`
- `gir1.2-webkit-6.0`
- `libwebkitgtk-6.0-4`
- FastAPI
- Uvicorn
- Pydantic

GTK / WebKitGTK / PyGObjectをpipからビルドする方式は採用しない。

現在のvenvはシステムの`gi`を参照できないため、実装時に次のいずれかへ移行する。

1. Ubuntu提供のPythonパッケージへ統一し、`/usr/bin/python3`で実行する。
2. GTK関連をシステムパッケージで導入し、`--system-site-packages`付きvenvを作成する。

対象OSをUbuntu 26.04に限定するため、第一候補はシステムパッケージへの統一とする。実装開始前に必要なFastAPI等の提供バージョンと既存テストの互換性を再確認する。

## 12. 既存UIの維持と変更

### 維持するもの

- カフェメニュー風の配色と紙風デザイン
- ライト / ダークテーマ
- 1～3列のレスポンシブグリッド
- 一覧、Group / Tag、ソート、Tag絞り込み
- 新規作成、編集、削除モーダル
- ホスト死活状態表示
- 実行不可ボタンとツールチップ

### 変更するもの

- Google Fontsの外部読込を廃止する。
- デスクトップモードではトークン入力モーダルを表示しない。
- 起動中、接続失敗、再接続の表示を追加する。
- Run操作をWebKitGTKのネイティブメッセージへ接続する。
- 必要に応じてウィンドウ全面表示向けに余白を微調整する。
- ブラウザ固有の文言や手順を削除する。

## 13. ファイル構成案

実装後の構成は次を目安とする。既存の「バックエンドは単一ファイル」という制約は、デスクトップ化に伴い見直す。

```text
app/
├── main.py                 # FastAPIアプリと互換Web起動
├── desktop.py              # GTKアプリのエントリーポイント
├── runtime.py              # 動的ポート・接続情報・サービスライフサイクル
├── terminal.py             # GTK側の安全なターミナル起動
└── static/
    ├── index.html
    ├── app.js
    ├── style.css
    └── icons...

resources/
├── script-runner.desktop.in
└── icons/
    └── script-runner.svg

tests/
├── 既存テスト
├── test_runtime_connection.py
├── test_desktop_bootstrap.py
├── test_terminal_launch.py
└── test_installer_desktop.py
```

ファイル分割は責務分離を目的とし、過度な細分化は避ける。最終的な名称は実装開始時に既存コードとの依存関係を確認して確定する。

## 14. 既存ファイルの整理方針

不要になったファイルや旧実装は、その場で即時削除せず、移行と検証の期間だけプロジェクトルートの`old/`配下へ退避する。

### 14.1 退避の目的

- 新旧動作の比較
- 移行漏れの確認
- インストール・アンインストール処理の参照
- 問題発生時の一時的な切り戻し

### 14.2 退避時の規則

- 移動理由と移動元を`old/README.md`へ記録する。
- 退避ファイルを実行経路、テスト、パッケージ、インストーラーから参照しない。
- 秘密情報、実ユーザー設定、バックアップデータを`old/`へ入れない。
- `old/`を恒久的な互換レイヤーにしない。
- 同名ファイルがある場合は由来が分かる名前にする。

### 14.3 削除条件

以下をすべて満たした段階で`old/`と退避ファイルを削除する。

- GTKアプリから一覧・作成・編集・削除・実行が正常に動作する。
- ユーザーサービスのインストール、更新、再起動、アンインストールが正常に動作する。
- 動的ポートと接続情報の異常系テストが完了している。
- WaylandおよびX11の受け入れテストが完了している。
- 旧ファイルへの参照が`rg`等で存在しないことを確認している。
- ドキュメントが新構成に更新されている。
- 必要な移行情報が変更履歴またはドキュメントへ転記されている。

最終成果物に`old/`を残さない。

## 15. インストーラー変更計画

### 15.1 前提条件確認

- Ubuntu 26.04であることを確認する。
- GTK 4とWebKitGTK 6.0のGI名前空間をPythonから読み込めることを確認する。
- 不足するAPTパッケージを具体的に案内する。
- インストーラー自身は無断でsudoやAPTを実行しない。

### 15.2 インストール処理

- アプリケーションファイルを配置する。
- `config.json`と既存スクリプト保存先を維持する。
- systemdユーザーサービスを生成・配置する。
- 固定ポート入力を削除する。
- GUI環境変数を`.env`へ書き込む処理を削除する。
- 通常デスクトップ利用向けのSECRET_TOKEN入力を削除する。
- デスクトップエントリーとアイコンを配置する。
- systemdユーザーデーモンを再読み込みする。
- サービスを有効化・起動する。
- `connection.json`とヘルスチェックを確認する。
- `loginctl enable-linger`を実行しない。
- 完了時にアプリケーションメニューからの起動方法を表示する。

### 15.3 更新処理

- 旧サービスを安全に停止する。
- 既存設定と外部スクリプト保存先を保持する。
- 古い`.env`から必要な設定だけを移行する。
- 不要になった固定ポート・GUI環境設定を破棄する。
- 新サービス定義へ置換する。
- サービス再起動後に新しい接続情報を確認する。
- 起動中のGTKアプリがある場合は再接続できるようにする。

### 15.4 ロールバック

- 新規配置したデスクトップエントリーとアイコンを削除する。
- 新サービス定義の配置に失敗した場合、変更前のサービス定義を復元する。
- `config.json`とスクリプト保存先を破壊しない。
- 外部スクリプトディレクトリを削除しない。
- 接続情報ファイルをバックアップ対象にせず、サービス再起動で再生成する。

## 16. アンインストーラー変更計画

- GTKアプリが起動中の場合は終了を案内する。
- FastAPIユーザーサービスを停止・無効化する。
- systemdユーザーサービスファイルを削除する。
- デスクトップエントリーとアイコンを削除する。
- アプリケーションメニューとアイコンキャッシュを必要に応じて更新する。
- `$XDG_RUNTIME_DIR/script-runner/`を所有者と内容を確認したうえで削除する。
- プロジェクト内スクリプトを削除する前に既存どおりバックアップ確認を行う。
- 外部スクリプトディレクトリは削除しない。
- `config.json`等のバックアップ方針を現行仕様から引き継ぐ。
- 旧サービス、旧デスクトップエントリー、旧アイコンが残っていれば対象を明示して除去する。

## 17. 実装フェーズ

### フェーズ0: ベースライン確定

- 現在の全テスト、Python / JavaScript / Shell構文検査を実行する。
- 現行サービス、設定、インストーラーの動作を記録する。
- Ubuntu 26.04の必要パッケージと名前空間を確定する。
- 変更対象と退避候補を一覧化する。

完了条件:

- 現行動作と既知の問題が文書化されている。
- 実装開始前の検証結果が保存されている。

### フェーズ1: バックエンドの責務分離

- GUI環境とターミナル起動への依存をFastAPIから分離する。
- デスクトップ実行用の安全な検証APIを設計する。
- 互換Webモードの扱いを確定する。
- 既存APIテストを維持する。

完了条件:

- バックエンドがGUI環境変数なしで動作する。
- 既存のファイル管理・メタデータ機能に回帰がない。

### フェーズ2: 動的ポートと接続情報

- バインド済みループバックソケットによる動的ポート起動を実装する。
- ランタイムディレクトリと接続情報の安全な作成・削除を実装する。
- 原子的書込み、権限、古いファイル、異常終了をテストする。
- systemdサービス定義を更新する。

完了条件:

- サービスを繰り返し再起動しても正しい接続情報が公開される。
- LANインターフェースでは待ち受けていない。
- 古い接続情報から誤接続しない。

### フェーズ3: GTK4 / WebKitGTKアプリ

- `Gtk.Application`を作成する。
- 最大化ウィンドウとWebKitビューを作成する。
- 接続情報読込、ヘルスチェック、再試行、エラー画面を実装する。
- 単一起動と既存ウィンドウの前面表示を実装する。
- ナビゲーション制限を実装する。

完了条件:

- 外部ブラウザなしで現在の一覧画面を表示できる。
- 起動時に最大化される。
- サービス停止時に理解可能なエラーを表示できる。

### フェーズ4: 認証とネイティブ実行

- ランタイムトークンと一時セッション認証を実装する。
- JavaScriptとGTK間のRunメッセージを実装する。
- GTK側の安全なターミナル起動を実装する。
- ホワイトリストとパス検証を二重に確認する。

完了条件:

- 通常利用でトークン入力を要求しない。
- 任意コマンドや保存先外ファイルを実行できない。
- GTKアプリ終了後も起動済みターミナルが継続する。

### フェーズ5: UI調整

- 外部フォント依存を削除する。
- デスクトップモードの不要な認証UIを除去する。
- 起動・再接続表示を追加する。
- 現行テーマとレスポンシブ表示を確認する。

完了条件:

- 現行デザインを維持している。
- オフラインでも表示が崩れない。
- ライト / ダークテーマが動作する。

### フェーズ6: インストール・移行・アンインストール

- Ubuntu 26.04向け依存関係確認を実装する。
- デスクトップエントリーとアイコンを導入する。
- 旧インストールからの移行を実装する。
- ロールバックを実装する。
- アンインストーラーを更新する。

完了条件:

- 新規インストール、上書き更新、アンインストールが成功する。
- 設定と外部スクリプトを失わない。
- アプリケーションメニューへの登録・解除が正しく反映される。

### フェーズ7: 旧ファイル整理

- 不要ファイルを`old/`へ退避する。
- 新構成からの参照がないことを確認する。
- 回帰試験完了後に`old/`を削除する。
- README、DOCUMENTS、変更履歴を更新する。

完了条件:

- 最終成果物に`old/`や不要な互換ファイルが残っていない。
- 現在の構成とドキュメントが一致している。

## 18. テスト計画

### 18.1 バックエンド

- 既存11件の自動テストを維持する。
- 動的ポートで正常に待ち受ける。
- `127.0.0.1`以外で待ち受けない。
- バインドとUvicorn引渡しの間に競合がない。
- 接続情報公開前は接続を開始しない。
- 不正な`XDG_RUNTIME_DIR`を拒否する。
- ランタイムディレクトリ・ファイル権限が正しい。
- 接続情報を原子的に更新する。
- 正常終了・異常終了・再起動後に古い情報を誤使用しない。
- 認証なし、古いトークン、改ざんCookieを拒否する。

### 18.2 GTK / WebKitGTK

- 起動時に最大化される。
- 二重起動で新規ウィンドウを増やさない。
- サービス起動済み、起動途中、停止中を正しく扱う。
- 接続情報破損時にクラッシュしない。
- 外部URL、ポップアップ、ダウンロードを制限する。
- WebKitプロセス異常終了時に復旧またはエラー表示する。
- ウィンドウ終了後にWebKit関連プロセスが残り続けない。

### 18.3 機能回帰

- 一覧表示
- 自然順ソート
- 更新日時ソート
- Groupソート
- Tag絞り込み
- Group / Tagメタデータ
- SSHホスト抽出
- 死活確認
- 新規作成
- 編集・保存
- 排他的作成
- 削除確認
- ホワイトリスト表示
- Run許可・拒否
- ライト / ダークテーマ

### 18.4 ターミナル起動

- Wayland環境で起動できる。
- X11フォールバックで起動できる。
- 対応ターミナルごとの引数配列が正しい。
- 空白・日本語を含むスクリプトパスを扱える。
- シェルインジェクションが起こらない。
- GTKアプリ終了後もターミナルが継続する。

### 18.5 インストール・運用

- 新規インストール
- 既存ブラウザ版からの更新
- サービス再起動
- ログアウト・再ログイン
- サービスの異常終了と自動復旧
- デスクトップエントリーからの起動
- アンインストール
- インストール途中のロールバック
- 外部スクリプトディレクトリの保持
- `old/`削除後の完全試験

## 19. ドキュメント更新

### README.md

- 操作手順を「アプリ起動→スクリプト選択」へ変更する。
- ブラウザと`localhost:8080`の通常利用手順を削除する。
- Ubuntu 26.04専用であることを明記する。
- 必要なAPTパッケージを記載する。
- systemdユーザーサービスとGTKアプリの役割を説明する。
- ログ確認、サービス再起動、接続エラー時の確認方法を記載する。

### DOCUMENTS.md

- 新アーキテクチャと責務境界を記載する。
- 動的ポートと`connection.json`の仕様を記載する。
- 認証、GTKメッセージ、ターミナル起動の仕様を記載する。
- インストール・移行・アンインストール仕様を更新する。
- 旧ブラウザ版の履歴は変更履歴として残す。

## 20. 完了条件

以下をすべて満たした時点でデスクトップアプリ化を完了とする。

- Ubuntu 26.04のアプリケーションメニューから起動できる。
- 外部ブラウザ、URL入力、お気に入りを使用しない。
- 起動時に最大化された専用ウィンドウが表示される。
- FastAPIはsystemdユーザーサービスとして動作する。
- サービスはループバックの動的ポートだけで待ち受ける。
- 接続情報が`$XDG_RUNTIME_DIR`へ安全に書き出される。
- GTKアプリが接続情報を使って自動接続する。
- GTKアプリ終了時もバックエンドサービスは継続する。
- ターミナル起動はGTKアプリ側で行われる。
- 一覧・作成・編集・削除・実行・分類・死活確認が動作する。
- 現行デザインが維持される。
- 通常利用で認証トークン入力が不要である。
- LANからバックエンドへ接続できない。
- インストール、更新、アンインストール、ロールバックが確認済みである。
- 外部スクリプトディレクトリと既存設定が保持される。
- 不要な既存ファイルと`old/`が最終成果物から削除されている。
- 自動テスト、構文検査、Wayland / X11実機試験が完了している。
- READMEとDOCUMENTSが実装と一致している。

## 21. Git運用

本計画の作成時点ではGit操作を行わない。

- コミット、ブランチ作成、タグ付け、pushは利用者の明示指示後に行う。
- 実装中のコミット単位や進捗記録方法も、利用者の指示を受けて確定する。
- Gitが利用可能になった場合も、`old/`への一時退避と最終削除を同一の不透明な変更にせず、追跡可能な段階に分ける。
